Your previous solution did not pass. Here is the result:

```
<%= @failure_output %>
```

Fix your solution. Reply with the complete corrected module (named exactly `<%= @module_name %>`) in a single ```elixir fenced code block. If you show more than one code block, only the LAST one is graded.
